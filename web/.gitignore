# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# 從 repo 根目錄 data/latest/ 複製過來的資料快照（見 scripts/copy-data.mjs）。
# 這裡故意不進版控——真正的資料來源是 repo 根目錄的 data/latest/，
# 這份只是每次 build/dev 前的暫時複製，避免兩份資料各自維護、彼此對不上。
/data/

# typescript
*.tsbuildinfo
next-env.d.ts
